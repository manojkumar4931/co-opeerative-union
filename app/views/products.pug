extends layout
block content
    - console.log(data)
    .main-content 
    h1 All products  
    table(border=1)
        for item in data 
            tr  
                td #{item.id}
                td 
                    a(href='/product-single/' + item.id) #{item.name}